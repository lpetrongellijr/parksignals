name: ParkSignals Monitor

on:
  workflow_dispatch:

permissions:
  contents: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
  PARKSIGNALS_POST_CONTEXT: "monitor"
  PARKSIGNALS_X_POSTING_ENABLED: ${{ vars.PARKSIGNALS_X_POSTING_ENABLED }}
  X_API_KEY: ${{ secrets.X_API_KEY }}
  X_API_SECRET: ${{ secrets.X_API_SECRET }}
  X_ACCESS_TOKEN: ${{ secrets.X_ACCESS_TOKEN }}
  X_ACCESS_TOKEN_SECRET: ${{ secrets.X_ACCESS_TOKEN_SECRET }}
  X_BEARER_TOKEN: ${{ secrets.X_BEARER_TOKEN }}

jobs:
  monitor:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Sync latest repo
        run: git pull --rebase origin main

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Dependencies
        run: pip install -r requirements.txt

      - name: Run ParkSignals
        run: |
          rm -rf outputs
          mkdir -p outputs
          python scripts/run_monitor.py | tee outputs/monitor-summary.txt
          python scripts/update_data_capture.py
          python scripts/export_artifacts.py --output-dir outputs
          python scripts/export_x_status.py --output-dir outputs
          python scripts/plan_posts.py --output-dir outputs --post-context monitor
          python scripts/dispatch_posts.py --output-dir outputs

      - name: Upload Monitor Outputs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: parksignals-monitor-outputs
          path: outputs/

      - name: Commit Updated State, Analytics History, And Posting Log
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add state.json analytics_history.json posting_log.json
          if git diff --cached --quiet; then
            echo "No state, analytics history, or posting log changes"
            exit 0
          fi
          git commit -m "Update ride state analytics history and posting log"
          git status --short
          git reset --hard HEAD
          git clean -fdX
          git fetch origin main
          git rebase origin/main
          git push
